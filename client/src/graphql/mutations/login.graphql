mutation Login($usernameOrEmail: String!, $password: String!, $socialMedia: String!, $token: String!) {
  login(usernameOrEmail: $usernameOrEmail, password: $password, socialMedia: $socialMedia, token: $token ) {
    ...RegularUserResponse
  }
}